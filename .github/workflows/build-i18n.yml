name: Build i18n and Package ModI18N

on:
  workflow_dispatch: {}

jobs:
  build_i18n:
    runs-on: windows-latest
    permissions:
      contents: read
    env:
      PARATRANZ_TOKEN: ${{ secrets.PARATRANZ_TOKEN }}
      PYTHONIOENCODING: utf-8
    outputs:
      i18n_version: ${{ steps.detect.outputs.i18n_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install loguru ujson emoji httpx python-dotenv

      - name: Build i18n (auto detect version from Paratranz export)
        run: |
          python build.py

      - name: Detect built version
        id: detect
        shell: pwsh
        run: |
          $dir = "translated_source"
          if (!(Test-Path $dir)) { throw "translated_source not found" }
          $ver = Get-ChildItem -Path $dir -Directory | Sort-Object LastWriteTime -Descending | Select-Object -First 1 -ExpandProperty Name
          echo "i18n_version=$ver" >> $env:GITHUB_OUTPUT
          Write-Host "Detected i18n version: $ver"

      - name: Upload i18n.json artifact
        uses: actions/upload-artifact@v4
        with:
          name: i18n-${{ steps.detect.outputs.i18n_version }}
          path: translated_source\${{ steps.detect.outputs.i18n_version }}\i18n.json
          if-no-files-found: error

  package_mod:
    runs-on: windows-latest
    permissions:
      contents: read
    needs: build_i18n
    env:
      I18N_VERSION: ${{ needs.build_i18n.outputs.i18n_version }}
    steps:
      - name: Download built i18n.json
        uses: actions/download-artifact@v4
        with:
          name: i18n-${{ env.I18N_VERSION }}
          path: i18n-src

      - name: Download latest ModI18N release zip
        uses: robinraju/release-downloader@v1.10
        with:
          repository: BlackTeaPie/Course-of-Temptation-Chinese-Localization
          latest: true
          fileName: "ModI18N*.zip"
          out-file-path: mod-src

      - name: Prepare Mod workspace and version values
        shell: pwsh
        run: |
          $asset = Get-ChildItem -Path "mod-src" -Filter "ModI18N*.zip" | Sort-Object LastWriteTime -Descending | Select-Object -First 1
          if (-not $asset) { throw "No ModI18N zip asset downloaded." }
          echo "MOD_ZIP=$($asset.FullName)" >> $env:GITHUB_ENV

          # Derive versions strictly from I18N_VERSION
          $i18n = $env:I18N_VERSION
          $numeric = $i18n -replace '-[a-zA-Z]+$',''
          if ($i18n -match '-([a-zA-Z]+)$') { $letter = $Matches[1] } else { $letter = "" }
          if ($letter) { $withLetter = "$numeric-$letter" } else { $withLetter = $numeric }
          $display = "v$numeric-chs-0.0.0"

          echo "PKG_VER_NUMERIC=$numeric"       >> $env:GITHUB_ENV
          echo "PKG_VER_LETTER=$letter"         >> $env:GITHUB_ENV
          echo "PKG_VER_WITH_LETTER=$withLetter" >> $env:GITHUB_ENV
          echo "PKG_VER_DISPLAY=$display"       >> $env:GITHUB_ENV

          Write-Host "I18N_VERSION=$i18n"
          Write-Host "PKG_VER_NUMERIC=$numeric"
          Write-Host "PKG_VER_LETTER=$letter"
          Write-Host "PKG_VER_WITH_LETTER=$withLetter"
          Write-Host "PKG_VER_DISPLAY=$display"

      - name: Extract ModI18N zip
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path "mod-work" -Force | Out-Null
          Expand-Archive -Path "${{ env.MOD_ZIP }}" -DestinationPath "mod-work" -Force

      - name: Replace i18n.json
        shell: pwsh
        run: |
          $src = "i18n-src\i18n.json"
          if (!(Test-Path $src)) { throw "Built i18n.json not found at $src" }
          $targets = Get-ChildItem -Path "mod-work" -Filter "i18n.json" -Recurse
          if (!$targets) { throw "No i18n.json found inside ModI18N zip" }
          foreach ($t in $targets) {
            Copy-Item -Path $src -Destination $t.FullName -Force
            Write-Host "Replaced: $($t.FullName)"
          }

      - name: Patch boot.json versions
        shell: pwsh
        run: |
          $boot = Get-ChildItem -Path "mod-work" -Filter "boot.json" -Recurse | Select-Object -First 1
          if (-not $boot) { throw "boot.json not found in extracted ModI18N" }
          $content = Get-Content -LiteralPath $boot.FullName -Raw

          $verLetter = $env:PKG_VER_WITH_LETTER
          $display = $env:PKG_VER_DISPLAY

          # Replace the '=...' variant
          $content = [regex]::Replace($content, '("version"\s*:\s*")=([^"]*)(")', { param($m) $m.Groups[1].Value + "=" + $verLetter + "-" + $m.Groups[3].Value })

          # Replace the 'v...' variant (not starting with '=')
          $content = [regex]::Replace($content, '("version"\s*:\s*")v[^"]*(")', { param($m) $m.Groups[1].Value + $display + $m.Groups[2].Value })

          Set-Content -LiteralPath $boot.FullName -Value $content -Encoding utf8
          Write-Host "Patched: $($boot.FullName)"

      - name: Install 7-Zip
        shell: pwsh
        run: |
          choco install 7zip -y
          $env:Path += ";C:\Program Files\7-Zip"
          7z i | Out-Null

      - name: Repack ModI18N zip (Deflate, level 6) with I18N versioned name
        shell: pwsh
        run: |
          $outDir = "dist"
          New-Item -ItemType Directory -Path $outDir -Force | Out-Null
          $finalName = "ModI18N-$($env:I18N_VERSION).zip"
          $outZip = Join-Path $outDir $finalName
          if (Test-Path $outZip) { Remove-Item $outZip -Force }
          Push-Location "mod-work"
          & 7z a -tzip -mx=6 -mm=Deflate -r "..\$outZip" * | Write-Output
          Pop-Location
          echo "MOD_OUT_ZIP=$outZip" >> $env:GITHUB_ENV
          echo "MOD_FINAL_NAME=$finalName" >> $env:GITHUB_ENV
          Write-Host "Created: $outZip"

      - name: Upload ModI18N zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: ModI18N-${{ env.I18N_VERSION }}
          path: ${{ env.MOD_OUT_ZIP }}
          if-no-files-found: error